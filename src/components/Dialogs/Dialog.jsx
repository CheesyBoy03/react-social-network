import React from 'react';
import s from './Dialogs.module.css';

const Dialogs = (props) => {
    return(
        <div сlassName={s.block}>
           Dialogs 
        </div>
    );
};

export default Dialogs;